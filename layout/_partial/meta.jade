mixin date(date)
  .date
    p= date.format(config.date_format)

mixin category(categories)

  if categories.length > 0
    .categories
      ul
        - categories.each(function(category){
          li
            a(href=config.root + category.path)
              = category.name
        - })
